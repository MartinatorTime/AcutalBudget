primary_region = "arn"

[build]
  dockerfile = "Dockerfile"

[[mounts]]
  source = "actual_data"
  destination = "/data"

[metrics]
port = 9091
path = "/metrics" # default for most prometheus exporters